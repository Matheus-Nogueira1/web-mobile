name: oitavo_trabalho_parcial
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.4

dependencies:
  flutter:
    sdk: flutter

  validatorless: ^1.2.5
  intl: ^0.20.2
  all_br_forms: ^1.0.1
  all_br_validations: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
